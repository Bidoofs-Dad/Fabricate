{{#each monsters as |beast| }}
        <div class="race-button">
                <h1>{{beast.name}} | {{beast.beastType}}</h1> <br>
                <p class="race-det">{{beast.challengeRating}} | {{beast.alignment}}</p> <br>
            </div>
        </div>
{{/each}}

