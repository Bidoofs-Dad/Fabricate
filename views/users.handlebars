{{#each users as |user| }}
    <div class="char-card">
        <div class="subrace-button">d {{user.email}} <br>
            <div>{{user.id}} {{user.password}}</div>
        </div>
    </div>
{{/each}}
